import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  button: {
    borderWidth: 1,
    borderColor: 'white',
    borderRadius: 10,
    padding: 20,
  },
  text: {
    color: 'white',
    fontSize: 20,
  },
});

export default styles;
